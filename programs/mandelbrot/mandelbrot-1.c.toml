[options]
initial = [ "-pipe", "-Wall", "-O3", "-fomit-frame-pointer", "-march=ivybridge", "-pthread", "mandelbrot.c", "-o", "mandelbrot.gcc_run", "-lm", "rm",]
extended = []

[dependencies]
path = "PosixPath('data/dependencies/c')"
names = []
